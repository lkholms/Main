<UserControl
    x:Class="InfoHub.Flyouts.About"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InfoHub.Flyouts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="90" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Rectangle Fill="{StaticResource SecondaryBrush}" />
        <TextBlock Foreground="{StaticResource SecondaryContrastBrush}" Text="About" FontFamily="Segoe UI" FontWeight="ExtraLight" FontSize="35" Margin="20" />
        <ScrollViewer Grid.Row="1" Background="White">
            <TextBlock x:Name="MyContent" TextWrapping="Wrap" FontFamily="Segoe UI" FontWeight="ExtraLight" FontSize="20" Margin="20" Foreground="Black">
                Version 2013.0403.1730
            </TextBlock>
        </ScrollViewer>
    </Grid>
</UserControl>
