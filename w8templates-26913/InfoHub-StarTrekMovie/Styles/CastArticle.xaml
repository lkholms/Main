<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Shared.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Name="CastTemplate">
        <Grid Background="{StaticResource PrimaryBrush}" Width="300">
            <StackPanel Orientation="Horizontal">
                <Image Source="{Binding Image}" />
                <StackPanel VerticalAlignment="Center" Margin="10" MaxWidth="180">
                    <TextBlock Text="{Binding Character}" TextWrapping="Wrap" FontSize="40" />
                    <TextBlock Text="{Binding Actor}" TextWrapping="Wrap" FontSize="20" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
